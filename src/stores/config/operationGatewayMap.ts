// import { useWorkingProjectStore } from '../workingProject'

const getOperationGatewayMapConfig = () => {
	// const store = useWorkingProjectStore()
	const operationGatewayMapConfig: Array<[string, boolean]> = [
	]
	return operationGatewayMapConfig
}

export default getOperationGatewayMapConfig
